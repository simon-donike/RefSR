{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"RefSR overview","text":"<p>RefSR is a compact PyTorch Lightning sandbox for reference-guided super-resolution that fuses Sentinel-2 (10\u202fm) spectral inputs with high-resolution SPOT panchromatic guidance. The goal is to quickly validate fusion ideas before scaling the model or data pipeline.</p>"},{"location":"#key-concepts","title":"Key concepts","text":"<ul> <li>Dual-branch encoding \u2013 dedicated encoders extract high-frequency PAN cues and aligned 10\u202fm features for Sentinel-2 and SPOT inputs.</li> <li>Cross-attention fusion \u2013 a transformer-style attention map links LR S2 context with reference texture to steer the SR head.</li> <li>Modular building blocks \u2013 deformable convolutions, residual blocks, and attention modules are factored into <code>model/blocks.py</code> for reuse.</li> </ul>"},{"location":"#data-flow","title":"Data flow","text":"<ol> <li>Sentinel-2 LR query, SPOT PAN HR reference, and an optional 10\u202fm PAN key are prepared by the data module defined in <code>data/example_data.py</code>.</li> <li><code>PanS2FusionSR</code> encodes LR/HR features, builds a spatial attention map, upsamples it to the HR grid, and fuses it with PAN features.</li> <li>The SR head reconstructs super-resolved Sentinel-2 predictions that match the PAN grid resolution.</li> </ol> <p>Use the navigation on the left to jump to setup instructions, architectural details, or the API reference.</p>"},{"location":"api/","title":"API Reference","text":"<p>MkDocs renders this section from the source docstrings using <code>mkdocstrings</code>. Keep the inline documentation up-to-date and re-run <code>mkdocs build</code> (or open the live preview) to refresh the content automatically. </p>"},{"location":"api/#model","title":"Model","text":""},{"location":"api/#model.model.PanS2FusionSR","title":"<code>model.model.PanS2FusionSR(s2_in_ch=4, pan_in_ch=1, base_ch=64, sr_scale=4, out_ch=4)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Reference-guided super-resolution model that fuses Sentinel-2 10 m inputs with high-resolution PAN features.</p>"},{"location":"api/#model.model.PanS2FusionSR--core-functionality","title":"Core functionality","text":"<ul> <li>Encodes HR PAN features and 10 m S2/PAN features separately.</li> <li>Computes a transformer-style cross-attention map between S2 (query) and   PAN-10 m (key/value) features.</li> <li>Upsamples the attention map to the HR grid and fuses it with HR PAN features.</li> <li>Reconstructs super-resolved Sentinel-2 outputs through the SR head.</li> </ul>"},{"location":"api/#model.model.PanS2FusionSR--parameters","title":"Parameters","text":"<p>s2_in_ch : int     Number of input Sentinel-2 channels (e.g., 4 for RGB-NIR). pan_in_ch : int     Number of PAN channels (typically 1). base_ch : int     Feature dimension used throughout encoders and attention. sr_scale : int     Logical LR\u2192HR scaling factor (grid relationship only). out_ch : int     Number of output SR channels.</p>"},{"location":"api/#model.model.PanS2FusionSR--forward-inputs","title":"Forward Inputs","text":"<p>s2_lr : torch.Tensor     Sentinel-2 LR tensor of shape <code>(B, C_s2, H, W)</code>. spot_pan_hr : torch.Tensor     HR PAN tensor of shape <code>(B, 1, H_hr, W_hr)</code>. spot_pan_lr : torch.Tensor, optional     10 m PAN tensor of shape <code>(B, 1, H, W)</code>. If omitted, downsampled from HR.</p>"},{"location":"api/#model.model.PanS2FusionSR--returns","title":"Returns","text":"<p>s2_sr : torch.Tensor     Super-resolved Sentinel-2 output, shape <code>(B, out_ch, H_hr, W_hr)</code>. att_map_10m : torch.Tensor     10 m attention map used for fusion, shape <code>(B, 1, H, W)</code>.</p> Source code in <code>model/model.py</code> <pre><code>def __init__(\n    self,\n    s2_in_ch: int = 4,  # e.g. RGBNIR\n    pan_in_ch: int = 1,  # panchromatic\n    base_ch: int = 64,\n    sr_scale: int = 4,\n    out_ch: int = 4,\n):\n    super().__init__()\n\n    # HR PAN encoder (top branch)\n    self.pan_encoder_hr = PanFeatureEncoder(\n        in_ch=pan_in_ch,\n        base_ch=base_ch,\n    )\n\n    # separate encoders for S2 and SPOT@10m (bottom branch)\n    self.s2_encoder_10m = S2Spot10mEncoder(\n        in_ch=s2_in_ch,  # for now: 4-band RFB-NIR\n        base_ch=base_ch,\n    )\n    self.spot_encoder_10m = S2Spot10mEncoder(\n        in_ch=pan_in_ch,  # 1 channel PAN\n        base_ch=base_ch,\n    )\n\n    # Cross-attention\n    self.cross_att = CrossAttentionMap()\n\n    # SR head: concat [pan_hr_feats, att_map_up]\n    self.sr_head = SRHead(\n        in_ch=base_ch + 1,\n        base_ch=base_ch,\n        out_ch=out_ch,\n        scale=1,  # pan_hr already at target 2.5 m grid\n    )\n</code></pre>"},{"location":"api/#encoders","title":"Encoders","text":""},{"location":"api/#model.encoders.PanFeatureEncoder","title":"<code>model.encoders.PanFeatureEncoder(in_ch=1, base_ch=64, n_resblocks=2)</code>","text":"<p>               Bases: <code>Module</code></p> <p>High-resolution PAN feature encoder used in the top branch of the network.</p> <p>This encoder processes high-resolution SPOT panchromatic imagery and extracts rich spatial features suitable for fusion with Sentinel-2 inputs. It applies a deformable convolution for geometric flexibility, followed by residual refinement and spatial attention to emphasize salient structures (e.g., edges, textures, fine details).</p>"},{"location":"api/#model.encoders.PanFeatureEncoder--parameters","title":"Parameters","text":"<p>in_ch : int, default=1     Number of input channels (typically 1 for PAN imagery). base_ch : int, default=64     Number of feature channels produced by the encoder. n_resblocks : int, default=2     Number of residual refinement blocks applied after the initial convolution.</p>"},{"location":"api/#model.encoders.PanFeatureEncoder--notes","title":"Notes","text":"<ul> <li>The deformable convolution expands the ability to adapt to local geometry   misalignments between modalities.</li> <li>Residual blocks increase nonlinear representational capacity without altering   spatial size.</li> <li>Spatial attention enhances local regions with strong structural cues, helping   the model focus on informative PAN features.</li> </ul> Source code in <code>model/encoders.py</code> <pre><code>def __init__(self, in_ch=1, base_ch=64, n_resblocks=2):\n    super().__init__()\n    self.head = nn.Sequential(\n        DeformableConv2d(in_ch, base_ch, kernel_size=3, padding=1),\n        nn.BatchNorm2d(base_ch),\n        nn.ReLU(inplace=True),\n    )\n    self.resblocks = nn.Sequential(\n        *[ResidualBlock(base_ch) for _ in range(n_resblocks)]\n    )\n    self.spatial_att = SpatialAttention(kernel_size=7)\n</code></pre>"},{"location":"api/#model.encoders.PanFeatureEncoder.forward","title":"<code>forward(x)</code>","text":"<p>Forward pass of the PAN feature encoder.</p>"},{"location":"api/#model.encoders.PanFeatureEncoder.forward--parameters","title":"Parameters","text":"<p>x : torch.Tensor     High-resolution PAN tensor of shape <code>(B, in_ch, H, W)</code>.</p>"},{"location":"api/#model.encoders.PanFeatureEncoder.forward--returns","title":"Returns","text":"<p>torch.Tensor     Refined PAN feature map of shape <code>(B, base_ch, H, W)</code>,     enriched with spatial attention weighting.</p> Source code in <code>model/encoders.py</code> <pre><code>def forward(self, x):\n    \"\"\"\n    Forward pass of the PAN feature encoder.\n\n    Parameters\n    ----------\n    x : torch.Tensor\n        High-resolution PAN tensor of shape ``(B, in_ch, H, W)``.\n\n    Returns\n    -------\n    torch.Tensor\n        Refined PAN feature map of shape ``(B, base_ch, H, W)``,\n        enriched with spatial attention weighting.\n    \"\"\"\n    x = self.head(x)\n    x = self.resblocks(x)\n    x = self.spatial_att(x)\n    return x\n</code></pre>"},{"location":"api/#model.encoders.S2Spot10mEncoder","title":"<code>model.encoders.S2Spot10mEncoder(in_ch, base_ch=64)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Shared 10 m-resolution encoder used for both Sentinel-2 (query) features and downsampled SPOT PAN (key) features.</p> <p>This module extracts aligned mid-resolution representations from both modalities, enabling consistent feature spaces for cross-attention. The encoder applies a deformable convolution for geometric flexibility, followed by a stack of convolutional layers for local feature extraction, and a channel attention module to emphasize spectrally relevant channels.</p>"},{"location":"api/#model.encoders.S2Spot10mEncoder--parameters","title":"Parameters","text":"<p>in_ch : int     Number of input channels (e.g., 4 for RGB-NIR Sentinel-2, 1 for PAN-LR). base_ch : int, default=64     Number of feature channels used throughout the encoder.</p>"},{"location":"api/#model.encoders.S2Spot10mEncoder--notes","title":"Notes","text":"<ul> <li>The deformable convolution helps compensate for sub-pixel misalignments   between S2 and SPOT grids.</li> <li><code>MultiConv</code> expands the local receptive field while preserving resolution.</li> <li><code>ChannelAttention</code> adaptively reweights spectral channels based on   learned global context.</li> </ul> Source code in <code>model/encoders.py</code> <pre><code>def __init__(self, in_ch, base_ch=64):\n    super().__init__()\n    self.head = nn.Sequential(\n        DeformableConv2d(in_ch, base_ch, kernel_size=3, padding=1),\n        nn.BatchNorm2d(base_ch),\n        nn.ReLU(inplace=True),\n    )\n    self.body = MultiConv(base_ch, n_layers=3)\n    self.chan_att = ChannelAttention(base_ch)\n</code></pre>"},{"location":"api/#model.encoders.S2Spot10mEncoder.forward","title":"<code>forward(x)</code>","text":"<p>Forward pass of the shared 10 m encoder.</p>"},{"location":"api/#model.encoders.S2Spot10mEncoder.forward--parameters","title":"Parameters","text":"<p>x : torch.Tensor     Input tensor of shape <code>(B, in_ch, H, W)</code> at 10 m resolution.</p>"},{"location":"api/#model.encoders.S2Spot10mEncoder.forward--returns","title":"Returns","text":"<p>torch.Tensor     Feature map of shape <code>(B, base_ch, H, W)</code>, aligned for use as     query or key/value inputs to the cross-attention module.</p> Source code in <code>model/encoders.py</code> <pre><code>def forward(self, x):\n    \"\"\"\n    Forward pass of the shared 10 m encoder.\n\n    Parameters\n    ----------\n    x : torch.Tensor\n        Input tensor of shape ``(B, in_ch, H, W)`` at 10 m resolution.\n\n    Returns\n    -------\n    torch.Tensor\n        Feature map of shape ``(B, base_ch, H, W)``, aligned for use as\n        query or key/value inputs to the cross-attention module.\n    \"\"\"\n    x = self.head(x)\n    x = self.body(x)\n    x = self.chan_att(x)\n    return x\n</code></pre>"},{"location":"api/#building-blocks","title":"Building blocks","text":""},{"location":"api/#model.blocks.DeformableConv2d","title":"<code>model.blocks.DeformableConv2d(in_ch, out_ch, kernel_size=3, stride=1, padding=1, dilation=1, deformable_groups=1, bias=True, modulation=True)</code>","text":"<p>               Bases: <code>Module</code></p> <p>A lightweight PyTorch module wrapping <code>torchvision.ops.deform_conv2d</code> to provide learnable deformable 2D convolutions with optional modulation.</p> <p>This layer predicts spatial offsets (and optionally modulation masks) from the input feature map and applies them to a learned convolution kernel, enabling spatially adaptive receptive fields. It can be used as a drop-in replacement for <code>nn.Conv2d</code>.</p>"},{"location":"api/#model.blocks.DeformableConv2d--parameters","title":"Parameters","text":"<p>in_ch : int     Number of input channels. out_ch : int     Number of output channels. kernel_size : int or tuple of int, default=3     Size of the convolution kernel. stride : int or tuple of int, default=1     Convolution stride. padding : int or tuple of int, default=1     Zero-padding applied to the input. dilation : int or tuple of int, default=1     Dilation factor for the convolution kernel. deformable_groups : int, default=1     Number of independent deformable kernel groups. bias : bool, default=True     Whether to include a learnable bias term. modulation : bool, default=True     If True, predicts a per-location modulation mask in addition to offsets.</p>"},{"location":"api/#model.blocks.DeformableConv2d--notes","title":"Notes","text":"<ul> <li>Offsets are predicted via a 2D convolution producing   <code>2 * deformable_groups * kernel_h * kernel_w</code> channels.</li> <li>If modulation is enabled, an additional convolution predicts a mask in <code>[0, 1]</code>.</li> <li><code>torchvision</code> provides optimized CUDA/C++ kernels; this wrapper prepares   offset/mask tensors and parameters.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def __init__(\n    self,\n    in_ch: int,\n    out_ch: int,\n    kernel_size: int = 3,\n    stride: int = 1,\n    padding: int = 1,\n    dilation: int = 1,\n    deformable_groups: int = 1,\n    bias: bool = True,\n    modulation: bool = True,\n) -&gt; None:\n    super().__init__()\n\n    if isinstance(kernel_size, int):\n        kernel_size = (kernel_size, kernel_size)\n    if isinstance(stride, int):\n        stride = (stride, stride)\n    if isinstance(padding, int):\n        padding = (padding, padding)\n    if isinstance(dilation, int):\n        dilation = (dilation, dilation)\n\n    self.kernel_size: Tuple[int, int] = kernel_size\n    self.stride: Tuple[int, int] = stride\n    self.padding: Tuple[int, int] = padding\n    self.dilation: Tuple[int, int] = dilation\n    self.deformable_groups = deformable_groups\n    self.modulation = modulation\n\n    offset_channels = 2 * deformable_groups * kernel_size[0] * kernel_size[1]\n    self.offset_conv = nn.Conv2d(\n        in_ch,\n        offset_channels,\n        kernel_size,\n        stride=stride,\n        padding=padding,\n        dilation=dilation,\n        bias=True,\n    )\n\n    if modulation:\n        mask_channels = deformable_groups * kernel_size[0] * kernel_size[1]\n        self.mask_conv = nn.Conv2d(\n            in_ch,\n            mask_channels,\n            kernel_size,\n            stride=stride,\n            padding=padding,\n            dilation=dilation,\n            bias=True,\n        )\n    else:\n        self.mask_conv = None\n\n    self.weight = nn.Parameter(\n        torch.empty(out_ch, in_ch, kernel_size[0], kernel_size[1])\n    )\n    if bias:\n        self.bias = nn.Parameter(torch.zeros(out_ch))\n    else:\n        self.bias = None\n\n    self.reset_parameters()\n</code></pre>"},{"location":"api/#model.blocks.DeformableConv2d.forward","title":"<code>forward(x)</code>","text":"<p>Apply deformable convolution to the input tensor.</p>"},{"location":"api/#model.blocks.DeformableConv2d.forward--parameters","title":"Parameters","text":"<p>x : torch.Tensor     Input feature map of shape <code>(B, C_in, H, W)</code>.</p>"},{"location":"api/#model.blocks.DeformableConv2d.forward--returns","title":"Returns","text":"<p>torch.Tensor     Output tensor of shape <code>(B, C_out, H_out, W_out)</code> after applying     deformable convolution using learned offsets (and optional modulation).</p>"},{"location":"api/#model.blocks.DeformableConv2d.forward--notes","title":"Notes","text":"<ul> <li>Offsets are generated from <code>offset_conv(x)</code>.</li> <li>If modulation is enabled, a mask in <code>[0, 1]</code> is generated via <code>sigmoid</code>.</li> <li>The underlying operation delegates to <code>torchvision.ops.deform_conv2d</code>.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    \"\"\"\n    Apply deformable convolution to the input tensor.\n\n    Parameters\n    ----------\n    x : torch.Tensor\n        Input feature map of shape ``(B, C_in, H, W)``.\n\n    Returns\n    -------\n    torch.Tensor\n        Output tensor of shape ``(B, C_out, H_out, W_out)`` after applying\n        deformable convolution using learned offsets (and optional modulation).\n\n    Notes\n    -----\n    - Offsets are generated from ``offset_conv(x)``.\n    - If modulation is enabled, a mask in ``[0, 1]`` is generated via ``sigmoid``.\n    - The underlying operation delegates to ``torchvision.ops.deform_conv2d``.\n    \"\"\"\n    offset = self.offset_conv(x)\n    mask = None\n    if self.mask_conv is not None:\n        mask = torch.sigmoid(self.mask_conv(x))\n    return deform_conv2d(\n        x,\n        offset,\n        self.weight,\n        self.bias,\n        stride=self.stride,\n        padding=self.padding,\n        dilation=self.dilation,\n        mask=mask,\n    )\n</code></pre>"},{"location":"api/#model.blocks.DeformableConv2d.reset_parameters","title":"<code>reset_parameters()</code>","text":"<p>Initialize convolution weights, offsets, and optional modulation masks.</p> <ul> <li>Kernel weights are initialized with Kaiming uniform initialization.</li> <li>Offset and mask prediction layers are zero-initialized so the module   initially behaves like a standard convolution.</li> <li>If a bias term is present, it is initialized uniformly according to the   fan-in of the kernel.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def reset_parameters(self) -&gt; None:\n    \"\"\"\n    Initialize convolution weights, offsets, and optional modulation masks.\n\n    - Kernel weights are initialized with Kaiming uniform initialization.\n    - Offset and mask prediction layers are zero-initialized so the module\n      initially behaves like a standard convolution.\n    - If a bias term is present, it is initialized uniformly according to the\n      fan-in of the kernel.\n    \"\"\"\n\n    nn.init.kaiming_uniform_(self.weight, a=math.sqrt(5))\n    nn.init.constant_(self.offset_conv.weight, 0.0)\n    nn.init.constant_(self.offset_conv.bias, 0.0)\n    if self.mask_conv is not None:\n        nn.init.constant_(self.mask_conv.weight, 0.0)\n        nn.init.constant_(self.mask_conv.bias, 0.0)\n    if self.bias is not None:\n        fan_in, _ = nn.init._calculate_fan_in_and_fan_out(self.weight)\n        bound = 1 / math.sqrt(fan_in)\n        nn.init.uniform_(self.bias, -bound, bound)\n</code></pre>"},{"location":"api/#model.blocks.ResidualBlock","title":"<code>model.blocks.ResidualBlock(ch, kernel_size=3)</code>","text":"<p>               Bases: <code>Module</code></p> <p>A standard residual block consisting of two Conv\u2013BN layers with an intermediate ReLU activation, wrapped in a skip connection.</p> <p>This block preserves the spatial resolution and channel count of the input and is commonly used to improve gradient flow and stabilize deeper architectures.</p>"},{"location":"api/#model.blocks.ResidualBlock--parameters","title":"Parameters","text":"<p>ch : int     Number of input and output channels. kernel_size : int, default=3     Size of the convolution kernels. Padding is chosen to preserve spatial size.</p>"},{"location":"api/#model.blocks.ResidualBlock--returns","title":"Returns","text":"<p>torch.Tensor     Output tensor with the same shape as the input, computed as     <code>x + F(x)</code> where <code>F</code> is the stacked Conv\u2013BN\u2013ReLU\u2013Conv\u2013BN subnetwork.</p> Source code in <code>model/blocks.py</code> <pre><code>def __init__(self, ch, kernel_size=3):\n    super().__init__()\n    padding = kernel_size // 2\n    self.block = nn.Sequential(\n        nn.Conv2d(ch, ch, kernel_size, padding=padding),\n        nn.BatchNorm2d(ch),\n        nn.ReLU(inplace=True),\n        nn.Conv2d(ch, ch, kernel_size, padding=padding),\n        nn.BatchNorm2d(ch),\n    )\n</code></pre>"},{"location":"api/#model.blocks.SpatialAttention","title":"<code>model.blocks.SpatialAttention(kernel_size=7)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Spatial attention module that reweights feature maps based on aggregated channel-wise statistics.</p> <p>This block computes both the average-pooled and max-pooled spatial descriptors across channels, concatenates them, and feeds them through a small convolution to produce a spatial attention mask in the range <code>[0, 1]</code>. The mask is then applied multiplicatively to the input, enhancing informative spatial regions while suppressing less relevant ones.</p>"},{"location":"api/#model.blocks.SpatialAttention--parameters","title":"Parameters","text":"<p>kernel_size : int, default=7     Kernel size of the convolution used to generate the attention mask.     Padding is set automatically to preserve spatial resolution.</p>"},{"location":"api/#model.blocks.SpatialAttention--forward-inputs","title":"Forward Inputs","text":"<p>x : torch.Tensor     Input tensor of shape <code>(B, C, H, W)</code>.</p>"},{"location":"api/#model.blocks.SpatialAttention--forward-outputs","title":"Forward Outputs","text":"<p>torch.Tensor     Output tensor of the same shape as the input, computed as     <code>x * attention_mask</code>.</p>"},{"location":"api/#model.blocks.SpatialAttention--notes","title":"Notes","text":"<ul> <li>The attention mask is derived from two complementary descriptors:   mean-pooled and max-pooled activations over channels.</li> <li>This module refines spatial saliency without changing tensor dimensionality.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def __init__(self, kernel_size=7):\n    super().__init__()\n    padding = kernel_size // 2\n    self.conv = nn.Conv2d(2, 1, kernel_size, padding=padding)\n</code></pre>"},{"location":"api/#model.blocks.ChannelAttention","title":"<code>model.blocks.ChannelAttention(ch, reduction=16)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Channel attention module following the Squeeze-and-Excitation (SE) mechanism.</p> <p>This block adaptively recalibrates channel-wise feature responses by explicitly modeling inter-channel dependencies. It performs global average pooling to produce a compact channel descriptor, processes it through a small MLP, and outputs a set of per-channel weights in <code>[0, 1]</code>. These weights modulate the original feature map, amplifying informative channels and suppressing noisy ones.</p>"},{"location":"api/#model.blocks.ChannelAttention--parameters","title":"Parameters","text":"<p>ch : int     Number of input and output channels. reduction : int, default=16     Reduction ratio used in the bottleneck of the MLP. Controls the capacity     and computational cost of the attention module.</p>"},{"location":"api/#model.blocks.ChannelAttention--forward-inputs","title":"Forward Inputs","text":"<p>x : torch.Tensor     Input tensor of shape <code>(B, C, H, W)</code>.</p>"},{"location":"api/#model.blocks.ChannelAttention--forward-outputs","title":"Forward Outputs","text":"<p>torch.Tensor     Output tensor of the same shape as the input, reweighted by learned     channel-wise importance coefficients.</p>"},{"location":"api/#model.blocks.ChannelAttention--notes","title":"Notes","text":"<ul> <li>Global average pooling produces a <code>(B, C)</code> descriptor.</li> <li>The MLP applies a bottleneck transformation and outputs <code>C</code> sigmoid-activated   weights.</li> <li>Broadcasting the resulting weights over spatial dimensions yields a modulation   mask that scales each channel independently.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def __init__(self, ch, reduction=16):\n    super().__init__()\n    self.avg_pool = nn.AdaptiveAvgPool2d(1)\n    self.mlp = nn.Sequential(\n        nn.Linear(ch, ch // reduction, bias=False),\n        nn.ReLU(inplace=True),\n        nn.Linear(ch // reduction, ch, bias=False),\n        nn.Sigmoid(),\n    )\n</code></pre>"},{"location":"api/#model.blocks.MultiConv","title":"<code>model.blocks.MultiConv(ch, n_layers=3, kernel_size=3)</code>","text":"<p>               Bases: <code>Module</code></p> <p>A lightweight convolutional feature extractor composed of repeated <code>Conv2d \u2192 BatchNorm2d \u2192 ReLU</code> layers.</p> <p>This block allows stacking multiple identical convolutional layers to deepen feature processing while preserving spatial resolution and channel count. It is commonly used to add local nonlinear capacity without changing tensor dimensionality.</p>"},{"location":"api/#model.blocks.MultiConv--parameters","title":"Parameters","text":"<p>ch : int     Number of input and output channels for all convolution layers. n_layers : int, default=3     Number of repeated convolutional sub-layers. kernel_size : int, default=3     Convolution kernel size for each layer. Padding is set to preserve spatial size.</p>"},{"location":"api/#model.blocks.MultiConv--forward-inputs","title":"Forward Inputs","text":"<p>x : torch.Tensor     Input tensor of shape <code>(B, C, H, W)</code>.</p>"},{"location":"api/#model.blocks.MultiConv--forward-outputs","title":"Forward Outputs","text":"<p>torch.Tensor     Output tensor with identical shape to the input, after applying all     stacked convolutional layers.</p>"},{"location":"api/#model.blocks.MultiConv--notes","title":"Notes","text":"<ul> <li>All intermediate layers preserve spatial dimensions due to symmetric padding.</li> <li>Increasing <code>n_layers</code> increases the receptive field and nonlinear expressiveness.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def __init__(self, ch, n_layers=3, kernel_size=3):\n    super().__init__()\n    padding = kernel_size // 2\n    layers = []\n    for _ in range(n_layers):\n        layers += [\n            nn.Conv2d(ch, ch, kernel_size, padding=padding),\n            nn.BatchNorm2d(ch),\n            nn.ReLU(inplace=True),\n        ]\n    self.net = nn.Sequential(*layers)\n</code></pre>"},{"location":"api/#model.blocks.SRHead","title":"<code>model.blocks.SRHead(in_ch, base_ch=64, out_ch=4, scale=1)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Super-resolution reconstruction head combining deformable convolution, residual refinement, and optional pixel-shuffle upsampling.</p> <p>This module takes fused high-resolution features and produces the final super-resolved output. It first applies a deformable convolution to align and normalize feature geometry, refines the representation using two residual blocks, optionally upsamples the feature map using a pixel-shuffle block, and finally maps the features into the desired number of output channels.</p>"},{"location":"api/#model.blocks.SRHead--parameters","title":"Parameters","text":"<p>in_ch : int     Number of channels in the fused input features. base_ch : int, default=64     Number of feature channels used within the SR head. out_ch : int, default=4     Number of output channels (e.g., target Sentinel-2 bands). scale : int, default=1     Spatial upsampling factor. If <code>scale &gt; 1</code>, a pixel-shuffle upsampling     stage is applied; otherwise the block preserves spatial resolution.</p>"},{"location":"api/#model.blocks.SRHead--notes","title":"Notes","text":"<ul> <li>If <code>scale &gt; 1</code>, the head performs learned upsampling via   <code>Conv2d \u2192 PixelShuffle(scale)</code>.</li> <li>If <code>scale == 1</code>, the module acts as a pure refinement head without   changing spatial resolution.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def __init__(self, in_ch, base_ch=64, out_ch=4, scale=1):\n    super().__init__()\n    self.scale = scale\n\n    self.pre = nn.Sequential(\n        DeformableConv2d(in_ch, base_ch, kernel_size=3, padding=1),\n        nn.BatchNorm2d(base_ch),\n        nn.ReLU(inplace=True),\n    )\n    self.body = nn.Sequential(\n        ResidualBlock(base_ch),\n        ResidualBlock(base_ch),\n    )\n\n    if scale &gt; 1:\n        self.upconv = nn.Conv2d(\n            base_ch, base_ch * (scale**2), kernel_size=3, padding=1\n        )\n        self.pixel_shuffle = nn.PixelShuffle(scale)\n    else:\n        self.upconv = nn.Identity()\n        self.pixel_shuffle = nn.Identity()\n\n    self.final = nn.Conv2d(base_ch, out_ch, kernel_size=3, padding=1)\n</code></pre>"},{"location":"api/#model.blocks.SRHead.forward","title":"<code>forward(x)</code>","text":"<p>Forward pass of the super-resolution head.</p>"},{"location":"api/#model.blocks.SRHead.forward--parameters","title":"Parameters","text":"<p>x : torch.Tensor     Input tensor of fused features with shape <code>(B, in_ch, H, W)</code>.</p>"},{"location":"api/#model.blocks.SRHead.forward--returns","title":"Returns","text":"<p>torch.Tensor     Super-resolved output tensor of shape     <code>(B, out_ch, H * scale, W * scale)</code>.     If <code>scale == 1</code>, the spatial resolution is preserved.</p>"},{"location":"api/#model.blocks.SRHead.forward--workflow","title":"Workflow","text":"<ol> <li>DeformableConv \u2192 BN \u2192 ReLU    Align and normalize fused features.</li> <li>ResidualBlock \u00d7 2    Local refinement and nonlinear enhancement.</li> <li>Upsampling (optional)    If <code>scale &gt; 1</code>, expand channels and apply PixelShuffle.</li> <li>Final 3\u00d73 Conv    Project refined features into target spectral channels.</li> </ol> Source code in <code>model/blocks.py</code> <pre><code>def forward(self, x):\n    \"\"\"\n    Forward pass of the super-resolution head.\n\n    Parameters\n    ----------\n    x : torch.Tensor\n        Input tensor of fused features with shape ``(B, in_ch, H, W)``.\n\n    Returns\n    -------\n    torch.Tensor\n        Super-resolved output tensor of shape\n        ``(B, out_ch, H * scale, W * scale)``.\n        If ``scale == 1``, the spatial resolution is preserved.\n\n    Workflow\n    --------\n    1. **DeformableConv \u2192 BN \u2192 ReLU**\n       Align and normalize fused features.\n    2. **ResidualBlock \u00d7 2**\n       Local refinement and nonlinear enhancement.\n    3. **Upsampling (optional)**\n       If ``scale &gt; 1``, expand channels and apply PixelShuffle.\n    4. **Final 3\u00d73 Conv**\n       Project refined features into target spectral channels.\n    \"\"\"\n    x = self.pre(x)\n    x = self.body(x)\n    x = self.upconv(x)\n    x = self.pixel_shuffle(x)\n    x = self.final(x)\n    return x\n</code></pre>"},{"location":"api/#model.blocks.CrossAttentionMap","title":"<code>model.blocks.CrossAttentionMap(dim=64, num_heads=4)</code>","text":"<p>               Bases: <code>Module</code></p> <p>Multi-head transformer-style cross-attention module that produces a single-channel spatial attention map.</p> <p>This block takes two feature maps (query and key/value) with identical spatial and channel dimensions, projects them into Q/K/V embeddings, and computes multi-head scaled dot-product attention over the flattened spatial domain. The attended features are projected back to the original channel dimension, collapsed to a single channel via a learnable 1\u00d71 convolution, and normalized with a spatial softmax to yield an attention map in which all locations sum to 1 per sample.</p>"},{"location":"api/#model.blocks.CrossAttentionMap--parameters","title":"Parameters","text":"<p>dim : int, default=64     Channel dimension of the input feature maps and internal Q/K/V     projections. num_heads : int, default=4     Number of attention heads. Must evenly divide <code>dim</code>.</p>"},{"location":"api/#model.blocks.CrossAttentionMap--notes","title":"Notes","text":"<ul> <li>Inputs are expected in <code>(B, C, H, W)</code> format, with <code>C == dim</code>.</li> <li>Attention is computed over the flattened spatial dimension <code>HW</code>,   enabling long-range dependencies across the entire feature map.</li> <li>The final output is a spatial probability distribution per sample,   suitable for weighing or masking high-resolution features downstream.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def __init__(self, dim=64, num_heads=4):\n    super().__init__()\n    assert dim % num_heads == 0, \"dim must be divisible by num_heads\"\n\n    self.dim = dim\n    self.num_heads = num_heads\n    self.head_dim = dim // num_heads\n    self.scale = self.head_dim**-0.5\n\n    # Linear projections for Q, K, V\n    self.q_proj = nn.Conv2d(dim, dim, kernel_size=1)\n    self.k_proj = nn.Conv2d(dim, dim, kernel_size=1)\n    self.v_proj = nn.Conv2d(dim, dim, kernel_size=1)\n\n    # Output projection\n    self.out_proj = nn.Conv2d(dim, dim, kernel_size=1)\n\n    # Collapse C \u2192 1\n    self.to_map = nn.Conv2d(dim, 1, kernel_size=1)\n</code></pre>"},{"location":"api/#model.blocks.CrossAttentionMap.forward","title":"<code>forward(q, k)</code>","text":"<p>Compute a 1-channel spatial attention map from query and key/value features.</p>"},{"location":"api/#model.blocks.CrossAttentionMap.forward--parameters","title":"Parameters","text":"<p>q : torch.Tensor     Query feature map of shape <code>(B, C, H, W)</code>, typically derived from     the Sentinel-2 branch. k : torch.Tensor     Key/value feature map of shape <code>(B, C, H, W)</code>, typically derived     from the reference (e.g., PAN) branch.</p>"},{"location":"api/#model.blocks.CrossAttentionMap.forward--returns","title":"Returns","text":"<p>torch.Tensor     Attention map of shape <code>(B, 1, H, W)</code>, where each <code>H\u00d7W</code> map is a     spatial probability distribution (values in <code>[0, 1]</code> that sum to 1     per sample).</p>"},{"location":"api/#model.blocks.CrossAttentionMap.forward--notes","title":"Notes","text":"<ul> <li>Q, K, and V are obtained via learned 1\u00d71 convolutions.</li> <li>Multi-head attention is applied over the flattened spatial dimension   with standard scaled dot-product attention.</li> <li>The attended features are merged across heads, projected back to   <code>dim</code> channels, collapsed to a single channel via <code>to_map</code>, and   normalized with a spatial softmax.</li> </ul> Source code in <code>model/blocks.py</code> <pre><code>def forward(self, q, k):\n    \"\"\"\n    Compute a 1-channel spatial attention map from query and key/value features.\n\n    Parameters\n    ----------\n    q : torch.Tensor\n        Query feature map of shape ``(B, C, H, W)``, typically derived from\n        the Sentinel-2 branch.\n    k : torch.Tensor\n        Key/value feature map of shape ``(B, C, H, W)``, typically derived\n        from the reference (e.g., PAN) branch.\n\n    Returns\n    -------\n    torch.Tensor\n        Attention map of shape ``(B, 1, H, W)``, where each ``H\u00d7W`` map is a\n        spatial probability distribution (values in ``[0, 1]`` that sum to 1\n        per sample).\n\n    Notes\n    -----\n    - Q, K, and V are obtained via learned 1\u00d71 convolutions.\n    - Multi-head attention is applied over the flattened spatial dimension\n      with standard scaled dot-product attention.\n    - The attended features are merged across heads, projected back to\n      ``dim`` channels, collapsed to a single channel via ``to_map``, and\n      normalized with a spatial softmax.\n    \"\"\"\n    B, C, H, W = q.shape\n\n    # ---- Q, K, V projections ----\n    Q = self.q_proj(q)  # (B, C, H, W)\n    K = self.k_proj(k)\n    V = self.v_proj(k)\n\n    # ---- reshape to heads ----\n    # (B, heads, H*W, head_dim)\n    def reshape_heads(x):\n        x = x.view(B, self.num_heads, self.head_dim, H * W)\n        return x.permute(0, 1, 3, 2)  # (B, heads, HW, head_dim)\n\n    Qh = reshape_heads(Q)\n    Kh = reshape_heads(K)\n    Vh = reshape_heads(V)\n\n    # ---- scaled dot-product ----\n    # attn = softmax( Q @ K^T / sqrt(d) )\n    attn = (Qh @ Kh.transpose(-2, -1)) * self.scale  # (B, heads, HW, HW)\n    attn = attn.softmax(dim=-1)\n\n    # ---- output: (B, heads, HW, head_dim) ----\n    out = attn @ Vh\n\n    # ---- merge heads ----\n    out = out.permute(0, 1, 3, 2).contiguous()  # (B, heads, head_dim, HW)\n    out = out.view(B, C, H * W)\n    out = out.view(B, C, H, W)\n\n    # ---- output projection ----\n    out = self.out_proj(out)  # (B, C, H, W)\n\n    # ---- collapse C \u2192 1 via conv ----\n    att_map = self.to_map(out)  # (B,1,H,W)\n\n    # ---- spatial softmax ----\n    att_map = att_map.view(B, 1, -1)\n    att_map = att_map.softmax(dim=-1)\n    att_map = att_map.view(B, 1, H, W)\n\n    return att_map\n</code></pre>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#pans2fusionsr-pipeline","title":"PanS2FusionSR pipeline","text":"<p><code>model/model.py</code> defines <code>PanS2FusionSR</code>, a dual-branch fusion network:</p> <ol> <li>PAN HR encoder (<code>PanFeatureEncoder</code>) \u2013 extracts high-frequency cues from the SPOT 2.5\u202fm panchromatic image via deformable convolution, residual refinement, and spatial attention.</li> <li>Shared 10\u202fm encoders (<code>S2Spot10mEncoder</code>) \u2013 process Sentinel-2 LR inputs and downsampled PAN references into a common feature space enriched with channel attention.</li> <li>Cross-attention (<code>CrossAttentionMap</code>) \u2013 treats Sentinel-2 features as queries and PAN features as keys/values to yield a spatial attention map at the LR grid.</li> <li>SR head (<code>SRHead</code>) \u2013 upsamples the attention map, concatenates it with PAN HR features, and reconstructs the super-resolved Sentinel-2 output.</li> </ol>"},{"location":"architecture/#data-handling","title":"Data handling","text":"<p><code>data/example_data.py</code> implements a dummy Lightning <code>DataModule</code> that emits random tensors with the correct Sentinel-2/SPOT shapes. Replace it with domain loaders when integrating real scenes.</p>"},{"location":"architecture/#training-loop","title":"Training loop","text":"<p><code>train.py</code> initializes the configuration, data module, <code>PanS2System</code>, and PyTorch Lightning trainer. The system wraps the model, computes simple reconstruction losses, and exposes optimizer/scheduler hooks for experimentation.</p>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#install-dependencies","title":"Install dependencies","text":"<p>Use Python 3.9+ with PyTorch, torchvision, and PyTorch Lightning. The repo ships with a lightweight requirements file:</p> <pre><code>pip install -r requirements.txt\n</code></pre>"},{"location":"getting-started/#configure-an-experiment","title":"Configure an experiment","text":"<p><code>config/example_config.yaml</code> captures data, model, loss, and trainer hyperparameters in a single file. Update the paths or channel counts to match your Sentinel-2/SPOT assets.</p> <p>Key knobs: - <code>data</code> \u2013 batch size, number of workers, and dummy tensor shapes for smoke tests. - <code>model</code> \u2013 number of input/output channels plus the base feature width. - <code>trainer</code> \u2013 epochs, accelerator, logging cadence.</p>"},{"location":"getting-started/#run-the-trainer","title":"Run the trainer","text":"<p>Execute the Lightning entry point to launch a quick 2-epoch synthetic run:</p> <pre><code>python train.py\n</code></pre> <p>The script instantiates the OmegaConf config, builds the <code>ExampleRefSRDataModule</code>, and wires it to <code>PanS2FusionSR</code> along with a simple L1+L2 loss for debugging.</p>"}]}